// Course Architect — Assessment Lab
window.caAssessmentLab = function () {
    return {
        assessments: [],
        init() {
            if (window.lucide?.createIcons) window.lucide.createIcons();
        },
    };
};
