// Course Architect — Media Optimizer
window.caMediaOptimizer = function () {
    return {
        jobs: [],
        init() {
            if (window.lucide?.createIcons) window.lucide.createIcons();
        },
    };
};
