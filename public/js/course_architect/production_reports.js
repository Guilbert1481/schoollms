// Course Architect — Production Reports
window.caProductionReports = function () {
    return {
        rows: [],
        init() {
            if (window.lucide?.createIcons) window.lucide.createIcons();
        },
    };
};
